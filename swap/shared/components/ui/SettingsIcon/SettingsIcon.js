import React from 'react'

import settingsIcon from './images/settings.svg'


const SettingsIcon = ({ className }) => (
  <img className={className} src={settingsIcon} alt="settings icon" />
)

export default SettingsIcon
